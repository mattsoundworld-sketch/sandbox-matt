# starter_chat

Minimal composed app that uses reusable contracts from `libraries/ai_stack`.

## Run

From repo root:

```powershell
python -m apps.starter_chat.main
```

The run writes JSONL records to `data/processed/starter_chat_runs.jsonl`.
